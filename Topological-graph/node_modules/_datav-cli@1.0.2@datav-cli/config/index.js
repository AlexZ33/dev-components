'use strict';

var _ = require('lodash');
var path = require('path');
var envConfig = {};

try {
  envConfig = require('./config.js');
} catch (e) {
  // do nothing
}
var defaultConfig = {
  env: 'dev',
  debug: true,
  cacheDir: 'tmp_cache',
  iconDir: 'icons',
  resourceDir: 'resources',
  server: 'http://com.datav.aliyun.com/cube/',
  server_root: 'http://com.datav.aliyun.com/',
  root: path.join(__dirname, '../'),
  region: {
    INNER: "https://com.datav.alibaba-inc.com",
    CHN: "http://com.datav.aliyun.com"
  },
  regionDefault: 'CHN'
}
var config = _.merge({}, defaultConfig, envConfig);

module.exports = config;