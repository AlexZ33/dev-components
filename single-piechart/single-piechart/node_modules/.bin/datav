#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_datav-cli@0.2.1@datav-cli/bin/maliang.js" "$@"
  ret=$?
else 
  node  "$basedir/../_datav-cli@0.2.1@datav-cli/bin/maliang.js" "$@"
  ret=$?
fi
exit $ret
