#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_esminify@2.0.22@esminify/bin/esminify" "$@"
  ret=$?
else 
  node  "$basedir/../../../_esminify@2.0.22@esminify/bin/esminify" "$@"
  ret=$?
fi
exit $ret
