var _ = require('lodash');
var $ = require('jquery');
var React = require('react');
var ReactTab = require('react-tabs');
var Tab = ReactTab.Tab;
var Tabs = ReactTab.Tabs;
var TabList = ReactTab.TabList;
var TabPanel = ReactTab.TabPanel;

var Store = require('./store');
var Action = require('./action');
var Reflux = require('../lib/reflux');

var DataEditor = require('./rdata-editor');
var ConfigEditor = require('./rconfig-editor');
var PublishEditor = require('./rpublish-editor');
var ResizeEditor = require('./rresize-editor');


var Editor = React.createClass({
  mixins : [Reflux.connect(Store)],
  getInitialState: function() {
    return {
    }
  },
  _getSelectedComValidate: function () {
    if (!this.state.selectedComId) {
      return window.share.datav.validate;
    }
  },
  _getSelectedComConfig: function () {

  },
  render: function () {
    return (<Tabs style={{
      height: '100%'
    }}>
      <TabList>
        <Tab>样式</Tab>
        <Tab>数据</Tab>
        <Tab>缩放</Tab>
        <Tab>其他</Tab>
      </TabList>
      <TabPanel style={{
        position: 'relative',
        height: '100%'
      }}>
        <ConfigEditor/>
      </TabPanel>
      <TabPanel>
        <DataEditor/>
      </TabPanel>
      <TabPanel>
        <ResizeEditor/>
      </TabPanel>
      <TabPanel>
        <PublishEditor/>
      </TabPanel>
    </Tabs>)
  }
})

module.exports = Editor;


